export interface IUserUpdate {
  firstName: string;
  lastName: string;
  contactNumber: string;
  address: string;
}

export interface IUserUpdateResponse {
  firstName: string;
  lastName: string;
  address: string;
  contactNumber: string;
}
